import HeroStyled, { HeroContainer, HeroTitle } from './styles';

const Hero = () => (
  <HeroStyled>
    <HeroContainer>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </HeroContainer>
  </HeroStyled>
);

export default Hero;
